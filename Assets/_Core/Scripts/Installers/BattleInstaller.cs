using _Core.Scripts.Core.Battle;
using Core.PreBattle;
using Popups;
using UnityEngine;

namespace Installers
{
    public class BattleInstaller
    {
        [SerializeField] private BattleManager _battleManager;
        
        public void Bind()
        {
            /*binder.Bind(_battleManager);
            
            binder.Bind(FindObjectOfType<BattleUIPresenter>());
            binder.Bind(FindObjectOfType<TurnManager>());
            binder.Bind(FindObjectOfType<DiceTower>());
            binder.Bind(FindObjectOfType<SpellCaster>());
            binder.Bind(FindObjectOfType<BattlePreparation>());
            binder.Bind(FindObjectOfType<EnemySelectPanel>());
            
            binder.Bind(FindObjectOfType<PopupWin>());
            binder.Bind(FindObjectOfType<PopupLose>());*/
        }
    }
}